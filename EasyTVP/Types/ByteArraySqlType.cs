using System.Data;
using Microsoft.SqlServer.Server;
using System;
using System.Reflection;

namespace EasyTVP.Types
{
    //<TODO>
    //public class ByteArraySqlType : NullableSqlType<byte[]>
    //{
    //    protected override SqlMetaData GetSqlMetaData(PropertyInfo property)
    //    {
    //        return new SqlMetaData(property.Name, SqlDbType.VarChar);
    //    }

    //    protected override void SetRecord(SqlDataRecord record, int index, object value)
    //    {
    //        record.SetBytes(index, (byte[])value);
    //    }
    //}
}
